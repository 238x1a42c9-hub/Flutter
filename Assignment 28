Question 4: Show a gif in UI that is based on a timer. If the timer is equal and less than 15 seconds then show the gif else display a message like running time has been over. How will you solve this problem statement? 

personing running.gif

 

Hint 1:- Use futur.delayed for the duration

Hint 2:- Use a flag based on time and add logic to rendering the gif and text.
source code:
import 'package:flutter/material.dart';

void main() {
  runApp(GifTimerApp());
}

class GifTimerApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: GifTimerPage(),
    );
  }
}

class GifTimerPage extends StatefulWidget {
  @override
  _GifTimerPageState createState() => _GifTimerPageState();
}

class _GifTimerPageState extends State<GifTimerPage> {
  bool showGif = true;

  @override
  void initState() {
    super.initState();

    // Start a 15-second timer
    Future.delayed(Duration(seconds: 15), () {
      setState(() {
        showGif = false;
      });
    });
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("GIF Timer Example")),
      body: Center(
        child: showGif
            ? Image.asset(
                'assets/personing_running.gif', // make sure to add your GIF in pubspec.yaml
                width: 250,
                height: 250,
              )
            : Text(
                "Running time has been over",
                style: TextStyle(fontSize: 20, fontWeight: FontWeight.bold),
              ),
      ),
    );
  }
}
