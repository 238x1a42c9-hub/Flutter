Question 24: You have two buttons A & B . When you click on the A button the image will be shown and you click on the B button then your name, phone number, and email will be shown. How will you achieve this problem statement?

Hint 1:- Add the flag to show the different-2 data on click of buttons.

Hint 2:- Add text in the column for showing the name, phone number, and email.

Hint 3:- Add imageView to show the image.

Hint 4:- Add the image to the assets folder.

source code:
import 'package:flutter/material.dart';

void main() {
  runApp(const MaterialApp(
    debugShowCheckedModeBanner: false,
    home: ShowContentExample(),
  ));
}

class ShowContentExample extends StatefulWidget {
  const ShowContentExample({super.key});

  @override
  State<ShowContentExample> createState() => _ShowContentExampleState();
}

class _ShowContentExampleState extends State<ShowContentExample> {
  // Enum for managing display state
  DisplayMode _mode = DisplayMode.none;

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Show Image or Details")),
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            // Buttons
            Row(
              mainAxisAlignment: MainAxisAlignment.center,
              children: [
                ElevatedButton(
                  onPressed: () {
                    setState(() {
                      _mode = DisplayMode.image;
                    });
                  },
                  child: const Text("Show Image (A)"),
                ),
                const SizedBox(width: 20),
                ElevatedButton(
                  onPressed: () {
                    setState(() {
                      _mode = DisplayMode.details;
                    });
                  },
                  child: const Text("Show Details (B)"),
                ),
              ],
            ),

            const SizedBox(height: 40),

            // Conditional display area
            if (_mode == DisplayMode.image)
              Image.asset(
                'assets/my_image.png',
                width: 200,
                height: 200,
                fit: BoxFit.cover,
              )
            else if (_mode == DisplayMode.details)
              Column(
                children: const [
                  Text(
                    "Name: John Doe",
                    style: TextStyle(fontSize: 20, fontWeight: FontWeight.bold),
                  ),
                  Text(
                    "Phone: +1 9876543210",
                    style: TextStyle(fontSize: 18),
                  ),
                  Text(
                    "Email: johndoe@example.com",
                    style: TextStyle(fontSize: 18),
                  ),
                ],
              )
            else
              const Text(
                "Click a button to show content",
                style: TextStyle(fontSize: 18, color: Colors.grey),
              ),
          ],
        ),
      ),
    );
  }
}

// Enum to control which content is displayed
enum DisplayMode { none, image, details }
