Question 23: You are working on a Content-based application based on a flutter, client wants you to show a random lucky number on the click button. Which is the max limit is 350. What things will you require to achieve this Problem statement?

Hint 1:- Add the method for generating random lucky numbers.
Hint 2:- Add text and an Elevated button widget.

source code:
import 'package:flutter/material.dart';
import 'dart:math';

void main() {
  runApp(const MaterialApp(
    debugShowCheckedModeBanner: false,
    home: LuckyNumberApp(),
  ));
}

class LuckyNumberApp extends StatefulWidget {
  const LuckyNumberApp({super.key});

  @override
  State<LuckyNumberApp> createState() => _LuckyNumberAppState();
}

class _LuckyNumberAppState extends State<LuckyNumberApp> {
  int? luckyNumber; // Will store the generated number

  // Method to generate random lucky number up to 350
  void generateLuckyNumber() {
    final random = Random();
    setState(() {
      luckyNumber = random.nextInt(351); // Range: 0–350
    });
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Lucky Number Generator")),
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            Text(
              luckyNumber != null
                  ? "🎯 Your Lucky Number is: $luckyNumber"
                  : "Press the button to get your Lucky Number!",
              style: const TextStyle(
                fontSize: 22,
                fontWeight: FontWeight.bold,
              ),
              textAlign: TextAlign.center,
            ),
            const SizedBox(height: 30),
            ElevatedButton(
              onPressed: generateLuckyNumber,
              style: ElevatedButton.styleFrom(
                backgroundColor: Colors.deepPurple,
                padding:
                    const EdgeInsets.symmetric(horizontal: 30, vertical: 15),
              ),
              child: const Text(
                "Get Lucky Number",
                style: TextStyle(fontSize: 18, color: Colors.white),
              ),
            ),
          ],
        ),
      ),
    );
  }
}

